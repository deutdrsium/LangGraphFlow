# Trap Check Prompt (可解性检测，用轻量模型快速判断)
TRAP_CHECK_PROMPT="你是一位数学专家。请判断以下题目是否可解。检查以下问题：约束条件不足或矛盾导致无解/多解、逻辑漏洞、自相矛盾、要求过程性答案（如要求写出解题步骤而非数值结果）、术语定义不清等。注意：暴力计算题、需要编程求解的题目是被允许的，不算不可解。返回JSON对象：{\"solvable\": true/false, \"reason\": \"原因\"}，如果可解则reason为'pass'。"

# OpenAI API Configuration (or compatible third-party like DeepSeek/Qwen etc.)
OPENAI_BASE_URL=""
OPENAI_API_KEY=""

# Solve Prompt (纯代码生成，用强模型求解)
SOLVE_PROMPT="你是一位数学专家。请给出python程序代码求解此题（用```python```包裹）。你可以根据题目特点自由选择求解策略：sympy符号计算、暴力枚举、蒙特卡洛模拟、启发式算法等均可。对于几何题不要预设唯一解，可以预设多种情况分别求解；对于规模过大的题目，可以用小规模验证。"

# Retry Solve Prompt (二次独立验证，要求换思路)
RETRY_SOLVE_PROMPT="你是一位数学专家。现在需要你用与之前完全不同的方法重新求解一道题目。请务必采用不同的算法或数学方法（例如：如果之前用了符号计算，这次请用数值方法或蒙特卡洛；如果之前用了暴力枚举，这次请用解析方法）。直接给出python程序代码（用```python```包裹），不需要分析题目是否有陷阱。"

# Judge Prompt (代码执行结果初步裁判)
JUDGE_PROMPT="请你根据[Sandbox Execution Output]，判断[Ground Truth]是否是[Original Question]的正确答案。必须返回JSON对象，包含 'confidence' (整数置信度), 'decision' (必须是 'Match', 'Mismatch', 或 'Error'), 和 'verified_ans' (字符串，如果decision是'Match'则输出 'pass'，否则输出根据执行结果推导出的正确答案)。"

# Reasoning Solve Prompt (纯推理求解，不写代码，直接给出答案)
REASONING_SOLVE_PROMPT="你是一位数学专家。请用纯数学推理求解以下题目，不要编写代码。请给出完整的推理过程，最后在末尾用【最终答案】标记给出最终答案。请用中文输出。"

# Final Judge Prompt (综合代码和推理结果的最终裁判)
FINAL_JUDGE_PROMPT="你是一位数学裁判专家。请综合代码执行结果和纯推理结果，判断参考答案是否正确。必须返回JSON对象，包含 'confidence' (整数置信度), 'decision' (必须是 'Match', 'Mismatch', 或 'Error'), 和 'verified_ans' (字符串，如果decision是'Match'则输出 'pass'，否则用中文输出最终确认的正确答案)。"

MODEL_FLASH=gemini-3-flash-preview
MODEL_PRO=gemini-3.1-pro-preview